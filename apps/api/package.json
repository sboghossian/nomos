{
  "name": "@nomos/api",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny HTTP proxy for the Nomos playground: forwards extract<T> calls to OpenRouter with rate limiting.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && node --watch dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@nomos/core": "0.1.0",
    "@nomos/llm": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
